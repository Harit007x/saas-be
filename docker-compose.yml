services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: be-starter-app
    ports:
      - "${PORT:-5000}:5000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: always

# Optional: Add MongoDB if needed locally
#  mongodb:
#    image: mongo:latest
#    container_name: be-starter-db
#    ports:
#      - "27017:27017"
#    volumes:
#      - mongo-data:/data/db

# volumes:
#  mongo-data:
